import React from 'react';
import './spinner.scss';

const Spinner = () => {
    return (
        <React.Fragment>
            <div className="loader"></div>
        </React.Fragment>
    )
}

export default Spinner;
